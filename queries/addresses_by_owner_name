select
    replace("Owner Name 1", ', LLC', ' LLC') as name,
    count(*) as owner_count,
    group_concat("Tax Code" || ' | ' || "Site Address 1") as property_details
from "{year}"
where "Owner Occupied Exemption" = '6000'
group by name
having owner_count > 1
order by owner_count desc
