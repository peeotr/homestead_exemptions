select
    "Owner Name 1" as name,
    count("Owner Name 1") as owner_count,
    group_concat("Tax Code") as tax_codes
from "{year}"
where "Owner Occupied Exemption" = '6000'
group by name
having owner_count > 1
order by owner_count desc
